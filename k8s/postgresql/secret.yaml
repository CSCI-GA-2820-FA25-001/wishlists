# The way to get password and database_url:


# export POSTGRES_PASSWORD="wishlists"
# export DATABASE_URI="postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/wishlists"

# kubectl create secret generic postgres-creds \
#   --from-literal=password=$POSTGRES_PASSWORD \
#   --from-literal=database_uri=$DATABASE_URI

# kubectl get secret redis-creds -o yaml


# This secret can also be created from the command line using environment variables
#
# export DATABASE_URI='postgresql://<place-url-to-database-here>'
# export POSTGRES_PASSWORD='<place-password-here>'
#
# kubectl create secret generic postgres-creds \
#     --from-literal=password=$POSTGRES_PASSWORD
#     --from-literal=database_uri=$DATABASE_URI
#
apiVersion: v1
kind: Secret
metadata:
  name: postgres-creds
data:
  password: d2lzaGxpc3Rz
  database_uri: |
      cG9zdGdyZXNxbDovL3Bvc3RncmVzOndpc2hsaXN0c0Bwb3N0Z3Jlczo1NDMyL3dpc2hsaXN0cw==